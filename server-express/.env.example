# Express Server Configuration
PORT=8080

# Database Configuration
# Replace 'your_db_password' with a strong password
POSTGRES_URL=postgres://nexeed:your_db_password@localhost:5432/nexeed

# JWT Configuration
# Generate a strong, random secret key for production
JWT_SECRET=your_jwt_secret_key_here

# Session Secret
# Generate a strong, random secret key for session management
SESSION_SECRET=your_session_secret_key_here

# Frontend URL for CORS
# Update this to your actual frontend domain in production
FRONTEND_URL=https://your-frontend-domain.com

# FastAPI Service URL
AI_BASE=http://localhost:8001

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with actual configuration
# 3. Never commit .env file to version control
# 4. Use strong, unique passwords and secrets in production
